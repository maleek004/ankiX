AnkiX frontend (development scaffold)

To run locally:
1. cd src/frontend
2. npm install
3. npm run dev

The dev server proxies /api to http://localhost:5000. Adjust vite.config.js if your API runs on a different port.
